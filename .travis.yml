language: java

jdk:
  - oraclejdk8

before_script:
  - pip install --user codecov

script:
  - ./gradlew clean build

after_success:
  - codecov

before_cache:
  - rm -f $HOME/.gradle/caches/modules-2/modules-2.lock

cache:
  directories:
    - $HOME/.gradle/caches/
    - $HOME/.gradle/wrapper/
